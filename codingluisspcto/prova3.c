#include <stdio.h>

int main ()
{
    int x;
    printf("%d", x); /*alla variabile n il computer dà un valore aleatorio che non appartiene a nessun'altra variabile già usata, ma c'è il rischio che prenda una variabile nella memroia del pc*/
    return (0);
}